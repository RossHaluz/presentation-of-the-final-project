const { Description } = require('./HeroDesc.styled');

const HeroDesc = () => {
  return (
    <Description>
      we will find proven performers for your tasks in 15 minutes
    </Description>
  );
};

export default HeroDesc;
